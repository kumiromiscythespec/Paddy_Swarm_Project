# Dummy Panicle v0.1.3-panicle-head Material Guide

Every printable filename ends in `_TPU` or `_PETG` and is stored in the
matching material folder.  Do not infer material from part shape.  All fits
remain `CALIBRATION_PENDING`.

## TPU

- `panicle_tab_coupon_TPU.stl` — print 1 — TPU PANICLE-TAB-V001 tab with handle
- `DR-H01_panicle_branch_standard_TPU.stl` — print 6 — DR-H01 flat 10-branch, 14-grain TPU panicle panel

## PETG

- `stem_clearance_coupon_PETG.stl` — print 1 — PETG five-hole STEM-NOMINAL-4 clearance coupon
- `panicle_slot_coupon_PETG.stl` — print 1 — PETG five-slot PANICLE-TAB-V001 comparison coupon
- `DR-B01_root_socket_PETG.stl` — print 1 — DR-B01 one-piece MEDIUM 0-degree root socket
- `DR-S03_stem_end_plug_tube_id_3p6_calibration_PETG.stl` — print 1 — DR-S03 unverified 3.6 mm tube-ID calibration plug
- `DR-C01_cutting_cartridge_holder_PETG.stl` — print 2 — DR-C01 common upper/lower reusable cartridge holder
- `DR-C01_tube_marking_gauge_PETG.stl` — print 1 — DR-C01 removable paper-tube marking gauge; non-cut use only
- `DR-H02_panicle_hub_upright_PETG.stl` — print 1 — DR-H02 four-slot PETG panicle hub, fixed 0-degree interface
- `DR-H02_panicle_hub_droop20_PETG.stl` — print 1 — DR-H02 four-slot PETG panicle hub, fixed 20-degree interface
- `DR-H03_panicle_weight_cap_PETG.stl` — print 2 — DR-H03 removable PETG weight-pocket cap

The two files in `out/preview/` are non-printable ASSEMBLY STEP compounds and
have no STL counterpart.  Powered cutting remains unauthorized.
