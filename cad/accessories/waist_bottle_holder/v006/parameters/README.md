# Parameters

V006 の単一 authority は一つ上の `../parameters.json` です。生成スクリプトはそのファイルだけを読み、STEP、STL、validation report を再生成します。

実印刷 coupon で ID または slide clearance を確定した場合は、測定条件、プリンタ、材料、ノズル、layer、温度を記録してから `parameters.json` を更新してください。物理確認前に `physical_status` を PASS へ変更しないでください。
