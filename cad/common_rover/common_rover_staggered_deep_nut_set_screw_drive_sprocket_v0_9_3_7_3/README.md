# H2.4 staggered deep-nut set-screw v0.9.3.7.3

M3x25 headed screw and OD6.8 washer are removed. The recessed set screw clears the conservative link, but any usable circular access bore through the curved root removes 0.499080 mm3 from the protected root/link shell. The internal tool shank has the same conflict. H2.4 body/STLs are blocked references; coupons only may print. `H2.4_CAD = FAIL_ACCESS_BORE_LINK_CLEARANCE`; `H2.4_PHYSICAL_FIT = BLOCKED_PENDING_GEOMETRY_CORRECTION`; `GLOBAL_ASSEMBLY_INTERFERENCE = HOLD`; `PHYSICAL_PASS` is not granted; `POWERED_ROTATION = NOT_APPROVED`.
