# Image review

`PASS`, `CONDITIONAL_PASS`, and `FAIL` remain distinct. Only an unconditional
PASS receipt with zero blocking issues, zero reinspection requests, zero
critical NOT_VISIBLE items, complete image/hash/fingerprint/revision checks,
technical PASS, minimum-assembly PASS, and LOOP-001 PASS may unlock LOOP-002.
