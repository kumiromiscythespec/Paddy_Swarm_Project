# v2.29.3.9.1 correction report

- Lower adapter: `BOTTOM_SLOT`, host slots `RAIL-L-BOTTOM` / `RAIL-R-BOTTOM`.
- Upper torque: `XMEMBER-TOP`, normalized `TOP_SLOT`, semantic role
  `TOP_SLOT_OF_FRONT_CROSSMEMBER`.
- Join: explicit `slot_zone_id`; no fallback or component-name inference.
- Validation: source-relative and independent of current working directory.
